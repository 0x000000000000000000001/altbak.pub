#![allow(warnings)]
#![recursion_limit = "512"]
use perceus_ptr::PerceusPtr;
use purust_core::*;
use Purs_Bench::*;
use Purs_Control_Applicative::*;
use Purs_Control_Apply::*;
use Purs_Control_Bind::*;
use Purs_Control_Category::*;
use Purs_Control_Monad::*;
use Purs_Control_Semigroupoid::*;
use Purs_Data_Boolean::*;
use Purs_Data_CommutativeRing::*;
use Purs_Data_Eq::*;
use Purs_Data_EuclideanRing::*;
use Purs_Data_Function::*;
use Purs_Data_Functor::*;
use Purs_Data_HeytingAlgebra::*;
use Purs_Data_Monoid::*;
use Purs_Data_Ord::*;
use Purs_Data_Ordering::*;
use Purs_Data_Ring::*;
use Purs_Data_Semigroup::*;
use Purs_Data_Semiring::*;
use Purs_Data_Show::*;
use Purs_Data_Symbol::*;
use Purs_Data_Unit::*;
use Purs_Data_Void::*;
use Purs_Effect::*;
use Purs_Effect_Console::*;
use Purs_Record_Unsafe::*;
use Purs_Type_Proxy::*;
use Purs_Bench::*;
use Purs_Control_Applicative::*;
use Purs_Control_Bind::*;
use Purs_Data_Show::*;
use Purs_Effect::*;
use Purs_Effect_Console::*;

// Code generated by purust for module Test_ListOpsFFI



pub fn Test_ListOpsFFI_describe() -> crate::UnknownType {
    // AST: Typed(Typed(App(Var(...))))
std::thread_local! { static __PURUST_MODULE_VALUE: purust_core::module_values::Cell<crate::UnknownType> = const { purust_core::module_values::Cell::new() }; }
__PURUST_MODULE_VALUE.with(|slot| slot.get_or_init("Test_ListOpsFFI_describe", || {
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_log(/* Typed String <- String : Lit */String::from("List Processing FFI (900 elements):"))
}).clone())
}

pub fn Test_ListOpsFFI_act() -> crate::UnknownType {
    // AST: Typed(Let(...))
std::thread_local! { static __PURUST_MODULE_VALUE: purust_core::module_values::Cell<crate::UnknownType> = const { purust_core::module_values::Cell::new() }; }
__PURUST_MODULE_VALUE.with(|slot| slot.get_or_init("Test_ListOpsFFI_act", || {
{
    purust_core::Value::Func1(purust_core::Func1::Static(|mut _u: crate::UnknownType| -> crate::UnknownType {
        {
    let mut purs_local_0 = Bench_opaque(crate::mk_int(/* Typed i64 <- i64 : Lit */900));
    /* Typed crate::UnknownType <- crate::UnknownType : EffectBind */{
    let mut purs_local_1 = {
        let _val_eval = purs_local_0;
        if let purust_core::Value::Func1(f) = &_val_eval {
            f(purust_core::Value::Unit)
        } else if let purust_core::Value::Record_a(r) = &_val_eval {
            if r.call.is_some() {
                r.call.clone().unwrap()(purust_core::Value::Unit)
            } else {
                _val_eval
            }
        } else {
            _val_eval
        }
    };
    /* Typed crate::UnknownType <- crate::UnknownType : EffectPure */purust_core::Value::String(Data_Show_showIntImpl(/* Typed i64 <- i64 : App(Var(...)) */Test_ListOpsFFI_runListOpsFFI(/* Typed i64 <- crate::UnknownType : Local(...) */(purs_local_1).unwrap_int())))
}
}
    } as fn(crate::UnknownType) -> crate::UnknownType))
}
}).clone())
}



enum List {
    Nil,
    Cons(i64, Box<List>),
}

fn range_list(start: i64, end: i64) -> Box<List> {
    let mut acc = Box::new(List::Nil);
    let mut curr = end;
    while curr >= start {
        acc = Box::new(List::Cons(curr, acc));
        curr -= 1;
    }
    acc
}

fn filter_evens(mut lst: &List) -> Box<List> {
    let mut acc = Box::new(List::Nil);
    while let List::Cons(x, xs) = lst {
        if x % 2 == 0 {
            acc = Box::new(List::Cons(*x, acc));
        }
        lst = xs;
    }
    acc
}

fn foldl<F>(f: F, mut acc: i64, mut lst: &List) -> i64
where
    F: Fn(i64, i64) -> i64,
{
    while let List::Cons(x, xs) = lst {
        acc = f(acc, *x);
        lst = xs;
    }
    acc
}

pub fn Test_ListOpsFFI_runListOpsFFI(mut limit: i64) -> i64 {
    let lst = range_list(1, limit);
    let filtered = filter_evens(&lst);
    foldl(|a, b| a + b, 0, &filtered)
}




