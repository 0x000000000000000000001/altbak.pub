#![allow(warnings)]
#![recursion_limit = "512"]
use perceus_ptr::PerceusPtr;
use purust_core::*;
use Purs_Control_Applicative::*;
use Purs_Control_Apply::*;
use Purs_Control_Bind::*;
use Purs_Control_Category::*;
use Purs_Control_Monad::*;
use Purs_Control_Semigroupoid::*;
use Purs_Data_Boolean::*;
use Purs_Data_CommutativeRing::*;
use Purs_Data_Eq::*;
use Purs_Data_EuclideanRing::*;
use Purs_Data_Function::*;
use Purs_Data_Functor::*;
use Purs_Data_HeytingAlgebra::*;
use Purs_Data_Monoid::*;
use Purs_Data_Ord::*;
use Purs_Data_Ordering::*;
use Purs_Data_Ring::*;
use Purs_Data_Semigroup::*;
use Purs_Data_Semiring::*;
use Purs_Data_Show::*;
use Purs_Data_Symbol::*;
use Purs_Data_Unit::*;
use Purs_Data_Void::*;
use Purs_Effect::*;
use Purs_Record_Unsafe::*;
use Purs_Type_Proxy::*;
use Purs_Control_Applicative::*;
use Purs_Control_Bind::*;
use Purs_Data_Show::*;
use Purs_Effect::*;

// Code generated by purust for module Effect_Console



pub fn Effect_Console_warnShow(mut purs_local_0: std::rc::Rc<Purs_Data_Show::Show>, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_warn(/* Typed String <- String : App(Typed(Accessor(Local(...)))) */(/* Typed purust_core::Func1<crate::UnknownType, String> <- purust_core::Func1<crate::UnknownType, String> : Accessor(Local(...)) */(purs_local_0).show.clone())(/* Typed crate::UnknownType <- crate::UnknownType : Local(...) */purs_local_1))
}

pub fn Effect_Console_logShow(mut purs_local_0: std::rc::Rc<Purs_Data_Show::Show>, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_log(/* Typed String <- String : App(Typed(Accessor(Local(...)))) */(/* Typed purust_core::Func1<crate::UnknownType, String> <- purust_core::Func1<crate::UnknownType, String> : Accessor(Local(...)) */(purs_local_0).show.clone())(/* Typed crate::UnknownType <- crate::UnknownType : Local(...) */purs_local_1))
}

pub fn Effect_Console_infoShow(mut purs_local_0: std::rc::Rc<Purs_Data_Show::Show>, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_info(/* Typed String <- String : App(Typed(Accessor(Local(...)))) */(/* Typed purust_core::Func1<crate::UnknownType, String> <- purust_core::Func1<crate::UnknownType, String> : Accessor(Local(...)) */(purs_local_0).show.clone())(/* Typed crate::UnknownType <- crate::UnknownType : Local(...) */purs_local_1))
}

pub fn Effect_Console_grouped(mut purs_local_0: String, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Control_Bind_bind(Effect_bindEffect(), /* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_group(/* Typed String <- String : Local(...) */purs_local_0), /* Typed Abs */purust_core::Func1::Shared(std::rc::Rc::new(move |mut _a0: crate::UnknownType| -> crate::UnknownType {
    drop(_a0);
    {
    let mut purs_local_1 = purs_local_1.clone();
    purust_core::Value::Func1(purust_core::Func1::Shared(std::rc::Rc::new(move |mut _u: crate::UnknownType| -> crate::UnknownType {
// FREEVARS: purs_local_1
    let mut purs_local_1 = purs_local_1.clone();
        /* Typed crate::UnknownType <- crate::UnknownType : EffectBind */{
    let mut purs_local_3 = {
        let _val_eval = purs_local_1.clone();
        if let purust_core::Value::Func1(f) = &_val_eval {
            f(purust_core::Value::Unit)
        } else if let purust_core::Value::Record_a(r) = &_val_eval {
            if r.call.is_some() {
                r.call.clone().unwrap()(purust_core::Value::Unit)
            } else {
                _val_eval
            }
        } else {
            _val_eval
        }
    };
    {
        let _val_eval = /* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Control_Bind_bind(Effect_bindEffect(), /* Typed crate::UnknownType <- crate::UnknownType : Var(...) */Effect_Console_groupEnd(), /* Typed Abs */purust_core::Func1::Shared(std::rc::Rc::new(move |mut _a0: crate::UnknownType| -> crate::UnknownType {
    drop(_a0);
    {
    let mut purs_local_3 = purs_local_3.clone();
    purust_core::Value::Func1(purust_core::Func1::Shared(std::rc::Rc::new(move |mut _u: crate::UnknownType| -> crate::UnknownType {
// FREEVARS: purs_local_3
    let mut purs_local_3 = purs_local_3.clone();
        /* Typed crate::UnknownType <- crate::UnknownType : EffectPure */purs_local_3.clone()
    })))
}
})));
        if let purust_core::Value::Func1(f) = &_val_eval {
            f(purust_core::Value::Unit)
        } else if let purust_core::Value::Record_a(r) = &_val_eval {
            if r.call.is_some() {
                r.call.clone().unwrap()(purust_core::Value::Unit)
            } else {
                _val_eval
            }
        } else {
            _val_eval
        }
    }
}
    })))
}
})))
}

pub fn Effect_Console_errorShow(mut purs_local_0: std::rc::Rc<Purs_Data_Show::Show>, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_error(/* Typed String <- String : App(Typed(Accessor(Local(...)))) */(/* Typed purust_core::Func1<crate::UnknownType, String> <- purust_core::Func1<crate::UnknownType, String> : Accessor(Local(...)) */(purs_local_0).show.clone())(/* Typed crate::UnknownType <- crate::UnknownType : Local(...) */purs_local_1))
}

pub fn Effect_Console_debugShow(mut purs_local_0: std::rc::Rc<Purs_Data_Show::Show>, mut purs_local_1: crate::UnknownType) -> crate::UnknownType {
    // AST: Typed(Typed(Abs(..., Typed(Abs(..., Typed(Typed(App(Var(...)))))))))
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_debug(/* Typed String <- String : App(Typed(Accessor(Local(...)))) */(/* Typed purust_core::Func1<crate::UnknownType, String> <- purust_core::Func1<crate::UnknownType, String> : Accessor(Local(...)) */(purs_local_0).show.clone())(/* Typed crate::UnknownType <- crate::UnknownType : Local(...) */purs_local_1))
}



use std::collections::HashMap;
use std::io::{IsTerminal, Write};
use std::rc::Rc;
use std::sync::{Mutex, OnceLock};
use std::time::Instant;
use crate::UnknownType;

#[derive(Default)]
struct PurustConsoleState {
    indentation: usize,
    timers: HashMap<String, Instant>,
}

fn purust_console_effect(action: impl Fn(&mut PurustConsoleState) + 'static) -> UnknownType {
    static STATE: OnceLock<Mutex<PurustConsoleState>> = OnceLock::new();
    crate::Value::Func1(purust_core::Func1::Shared(Rc::new(move |_| {
        let mut state = STATE.get_or_init(|| Mutex::new(PurustConsoleState::default()))
            .lock().unwrap_or_else(|poisoned| poisoned.into_inner());
        action(&mut state);
        crate::Value::Unit
    })))
}

fn purust_console_write(state: &PurustConsoleState, message: &str, stderr: bool) {
    let message = purust_core::purust_string_to_utf8_lossy(message);
    let indentation = "  ".repeat(state.indentation);
    let output = format!("{}{}\n", indentation, message.replace('\n', &format!("\n{}", indentation)));
    if stderr {
        let _ = std::io::stderr().lock().write_all(output.as_bytes());
    } else {
        let _ = std::io::stdout().lock().write_all(output.as_bytes());
    }
}

fn purust_console_message(message: String, stderr: bool) -> UnknownType {
    purust_console_effect(move |state| purust_console_write(state, &message, stderr))
}

fn purust_console_warning(message: &str) {
    // Runtime warnings are immediate and independent of console groups.
    let _ = writeln!(std::io::stderr().lock(), "Warning: {}", purust_core::purust_string_to_utf8_lossy(message));
}

fn purust_console_timer(label: String, end: bool) -> UnknownType {
    purust_console_effect(move |state| {
        let started = if end {
            state.timers.remove(&label)
        } else {
            state.timers.get(&label).copied()
        };
        match started {
            Some(started) => {
                let message = format!("{}: {:.3}ms", label, started.elapsed().as_secs_f64() * 1000.0);
                purust_console_write(state, &message, false);
            }
            None => {
                let method = if end { "timeEnd" } else { "timeLog" };
                purust_console_warning(&format!("No such label '{}' for console.{}()", label, method));
            }
        }
    })
}

pub fn Effect_Console_log(message: String) -> UnknownType {
    purust_console_message(message, false)
}

pub fn Effect_Console_error(message: String) -> UnknownType {
    purust_console_message(message, true)
}

pub fn Effect_Console_warn(message: String) -> UnknownType {
    purust_console_message(message, true)
}

pub fn Effect_Console_info(message: String) -> UnknownType {
    purust_console_message(message, false)
}

pub fn Effect_Console_debug(message: String) -> UnknownType {
    purust_console_message(message, false)
}

pub fn Effect_Console_clear() -> UnknownType {
    purust_console_effect(|_| {
        let stdout = std::io::stdout();
        if stdout.is_terminal() && std::env::var("TERM").ok().as_deref() != Some("dumb") {
            let mut output = stdout.lock();
            let _ = output.write_all(b"\x1b[1;1H\x1b[0J");
            let _ = output.flush();
        }
    })
}

pub fn Effect_Console_group(label: String) -> UnknownType {
    purust_console_effect(move |state| {
        purust_console_write(state, &label, false);
        state.indentation += 1;
    })
}

pub fn Effect_Console_groupCollapsed(label: String) -> UnknownType {
    Effect_Console_group(label)
}

pub fn Effect_Console_groupEnd() -> UnknownType {
    purust_console_effect(|state| state.indentation = state.indentation.saturating_sub(1))
}

pub fn Effect_Console_time(label: String) -> UnknownType {
    purust_console_effect(move |state| {
        if state.timers.contains_key(&label) {
            purust_console_warning(&format!("Label '{}' already exists for console.time()", label));
        } else {
            state.timers.insert(label.clone(), Instant::now());
        }
    })
}

pub fn Effect_Console_timeEnd(label: String) -> UnknownType {
    purust_console_timer(label, true)
}

pub fn Effect_Console_timeLog(label: String) -> UnknownType {
    purust_console_timer(label, false)
}




