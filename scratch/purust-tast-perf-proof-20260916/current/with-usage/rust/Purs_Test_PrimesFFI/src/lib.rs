#![allow(warnings)]
#![recursion_limit = "512"]
use perceus_ptr::PerceusPtr;
use purust_core::*;
use Purs_Bench::*;
use Purs_Control_Applicative::*;
use Purs_Control_Apply::*;
use Purs_Control_Bind::*;
use Purs_Control_Category::*;
use Purs_Control_Monad::*;
use Purs_Control_Semigroupoid::*;
use Purs_Data_Boolean::*;
use Purs_Data_CommutativeRing::*;
use Purs_Data_Eq::*;
use Purs_Data_EuclideanRing::*;
use Purs_Data_Function::*;
use Purs_Data_Functor::*;
use Purs_Data_HeytingAlgebra::*;
use Purs_Data_Monoid::*;
use Purs_Data_Ord::*;
use Purs_Data_Ordering::*;
use Purs_Data_Ring::*;
use Purs_Data_Semigroup::*;
use Purs_Data_Semiring::*;
use Purs_Data_Show::*;
use Purs_Data_Symbol::*;
use Purs_Data_Unit::*;
use Purs_Data_Void::*;
use Purs_Effect::*;
use Purs_Effect_Console::*;
use Purs_Record_Unsafe::*;
use Purs_Type_Proxy::*;
use Purs_Bench::*;
use Purs_Control_Applicative::*;
use Purs_Control_Bind::*;
use Purs_Data_Show::*;
use Purs_Effect::*;
use Purs_Effect_Console::*;

// Code generated by purust for module Test_PrimesFFI



pub fn Test_PrimesFFI_describe() -> crate::UnknownType {
    // AST: Typed(Typed(App(Var(...))))
std::thread_local! { static __PURUST_MODULE_VALUE: purust_core::module_values::Cell<crate::UnknownType> = const { purust_core::module_values::Cell::new() }; }
__PURUST_MODULE_VALUE.with(|slot| slot.get_or_init("Test_PrimesFFI_describe", || {
/* Typed crate::UnknownType <- crate::UnknownType : App(Var(...)) */Effect_Console_log(/* Typed String <- String : Lit */String::from("Prime Sieve FFI (sum primes up to 500):"))
}).clone())
}

pub fn Test_PrimesFFI_act() -> crate::UnknownType {
    // AST: Typed(Let(...))
std::thread_local! { static __PURUST_MODULE_VALUE: purust_core::module_values::Cell<crate::UnknownType> = const { purust_core::module_values::Cell::new() }; }
__PURUST_MODULE_VALUE.with(|slot| slot.get_or_init("Test_PrimesFFI_act", || {
{
    purust_core::Value::Func1(purust_core::Func1::Static(|mut _u: crate::UnknownType| -> crate::UnknownType {
        {
    let mut purs_local_0 = Bench_opaque(crate::mk_int(/* Typed i64 <- i64 : Lit */500));
    /* Typed crate::UnknownType <- crate::UnknownType : EffectBind */{
    let mut purs_local_1 = {
        let _val_eval = purs_local_0;
        if let purust_core::Value::Func1(f) = &_val_eval {
            f(purust_core::Value::Unit)
        } else if let purust_core::Value::Record_a(r) = &_val_eval {
            if r.call.is_some() {
                r.call.clone().unwrap()(purust_core::Value::Unit)
            } else {
                _val_eval
            }
        } else {
            _val_eval
        }
    };
    /* Typed crate::UnknownType <- crate::UnknownType : EffectPure */purust_core::Value::String(Data_Show_showIntImpl(/* Typed i64 <- i64 : App(Var(...)) */Test_PrimesFFI_runPrimesFFI(/* Typed i64 <- crate::UnknownType : Local(...) */(purs_local_1).unwrap_int())))
}
}
    } as fn(crate::UnknownType) -> crate::UnknownType))
}
}).clone())
}



enum List {
    Nil,
    Cons(i64, Box<List>),
}

fn range_list(start: i64, end: i64) -> List {
    fn go_func(curr: i64, acc: List, start: i64) -> List {
        if curr < start {
            acc
        } else {
            go_func(curr - 1, List::Cons(curr, Box::new(acc)), start)
        }
    }
    go_func(end, List::Nil, start)
}

fn reverse(lst: List) -> List {
    fn go_func(list: List, acc: List) -> List {
        match list {
            List::Nil => acc,
            List::Cons(x, xs) => go_func(*xs, List::Cons(x, Box::new(acc)))
        }
    }
    go_func(lst, List::Nil)
}

fn filter<P>(p: &P, lst: List) -> List 
where P: Fn(i64) -> bool
{
    fn go_func<P>(p: &P, list: List, acc: List) -> List 
    where P: Fn(i64) -> bool 
    {
        match list {
            List::Nil => reverse(acc),
            List::Cons(x, xs) => {
                if p(x) {
                    go_func(p, *xs, List::Cons(x, Box::new(acc)))
                } else {
                    go_func(p, *xs, acc)
                }
            }
        }
    }
    go_func(p, lst, List::Nil)
}

fn sieve(lst: List) -> List {
    match lst {
        List::Nil => List::Nil,
        List::Cons(p, xs) => {
            let filtered = filter(&|x| x % p != 0, *xs);
            List::Cons(p, Box::new(sieve(filtered)))
        }
    }
}

fn sum_list(lst: List) -> i64 {
    fn go_func(list: List, acc: i64) -> i64 {
        match list {
            List::Nil => acc,
            List::Cons(x, xs) => go_func(*xs, acc + x)
        }
    }
    go_func(lst, 0)
}

pub fn Test_PrimesFFI_runPrimesFFI(limit: i64) -> i64 {
    sum_list(sieve(range_list(2, limit)))
}




