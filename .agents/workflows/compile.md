---
description: Fait compiler le code
---

Fais compiler le code. Tu peux utiliser la commande 'zsh -lc "./bin/build"', si besoin. Et pense aussi à ce qu'il n'y ait plus de warning (e.g. import mal fait).